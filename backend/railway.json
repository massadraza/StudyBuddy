{
  "services": [
    {
      "name": "backend",
      "build": {
        "builder": "dockerfile",
        "dockerfilePath": "./Dockerfile"
      },
      "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    }
  ]
}
